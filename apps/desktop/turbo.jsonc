{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist-electron/**"],
    },
    "dev": {
      "dependsOn": ["@studio/server#build"],
      "persistent": true,
    },
    "start": {
      "dependsOn": ["build", "@studio/web#build", "@studio/server#build"],
      "cache": false,
      "persistent": true,
    },
    "smoke-test": {
      "dependsOn": ["build", "@studio/web#build", "@studio/server#build"],
      "cache": false,
      "outputs": [],
    },
  },
}
